{
    
    "platform": {
        "apiRuntime": "node:16"
    },

    "routes":[
        {
            "route": "/api/*",
            "allowedRoles": ["authenticated"]
        },
        {
            "route": "/logout",
            "redirect": "/.auth/logout"
        }
    ]
}